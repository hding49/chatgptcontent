在Microsoft SQL Server Management Studio (SSMS) 中创建数据视图 (Data View) 可以通过以下步骤完成：

打开 SQL Server Management Studio 并连接到你的 SQL Server 数据库服务器。

在 "Object Explorer" 窗口中，展开你的数据库节点，以便在其中创建数据视图。

在 "数据库" 节点下，右键单击 "Views" 文件夹，然后选择 "New View"。这将打开一个新的查询窗口。

在 "Edit Top 200 Rows" 窗口中，你可以选择要包含在数据视图中的表格和列。你可以直接拖动表格和列到 "Diagram" 区域，或者手动编写 SQL 查询来选择需要的数据。

定义好你的数据视图后，点击工具栏上的 "Save" 按钮保存数据视图。你将需要为数据视图指定一个名称，并选择在哪个模式下保存它。

完成后，点击工具栏上的 "Execute SQL" 按钮，将数据视图保存到数据库中。

数据视图现在应该已经创建并保存在数据库中。你可以在 "Views" 文件夹下看到它。

请注意，创建数据视图是为了提供对现有数据表的查询和过滤功能，以便从数据库中检索特定的数据视图。你可以使用 SQL 查询来访问和使用数据视图中的数据。

在创建数据视图时，请确保你有足够的权限来执行这些操作，通常需要数据库管理员或具有创建视图权限的角色成员。